# 버전별 변경 재구성 (코드/리포트 기반)

이 문서는 **다른 채팅 로그 없이**, 현재 저장소의 커밋/리포트만으로 재구성한 내용이다.

- 확정 근거: `git log`, `git show`, `data/eval_report_v*.json`
- 추정 근거: 지표 변화 패턴 + 실패 질의 변화

## 1) 확인된 커밋 축

- `7d20101`  
  - 메시지: `chore: monorepo restructure and evaluation/parser improvements`
  - 특징: `src` 전반(`query_parser.py`, `ranker.py`, `llm_openai.py`, `evaluator.py` 등) + `eval_report_v1~v10`이 한 커밋에 포함
  - 해석: v1~v10의 세부 단계가 개별 커밋으로 남아있지 않고 압축되어 있음

- `abf9a21`  
  - 메시지: `Add explainable Streamlit/Dash dashboards and similar-to LLM fallback`
  - 확정 변경:
    - `apps/recommender/streamlit_app.py` 추가
    - `apps/recommender/dash_app.py` 추가
    - `apps/recommender/src/dashboard_formatter.py` 추가
    - `apps/recommender/src/llm_openai.py` / `ranker.py` 수정
    - `~같은 게임` DB 미매칭 시 LLM profile fallback 경로 추가

## 2) eval_report 기준 버전 흐름 요약

지표: `pass_rate / nonempty_rate / preferred_genre_hit_rate / hard_constraint_violation_rate`

- v1: `0.9167 / 0.75 / 0.75 / 0.0` (parse_fail 4, reco_fail 3)
- v2: `1.0 / 1.0 / 0.8333 / 0.1667` (0,2)
- v3: `1.0 / 1.0 / 0.8333 / 0.1667` (0,2)
- v4: `1.0 / 1.0 / 0.8333 / 0.1667` (0,2)
- v5: `1.0 / 1.0 / 0.8333 / 0.0` (0,0)
- v6: `1.0 / 0.8333 / 0.8333 / 0.0` (0,2)
- v7: `1.0 / 1.0 / 0.9167 / 0.0` (0,0)
- v8: `0.7375 / 1.0 / 0.9333 / 0.0167` (20,1)
- v9: `0.7833 / 1.0 / 1.0 / 0.0167` (20,1)
- v10: `0.8167 / 1.0 / 1.0 / 0.0` (20,0)

## 3) 버전 구간별 “왜 바뀌었는지” 재구성

아래는 **확정/추정**을 분리해 작성.

### v1 -> v2
- 확정: 파서 성공률/비어있지 않음/장르 적중률이 상승, 파싱 실패 4→0
- 확정: 동시에 하드 제약 위반률이 0→0.1667로 악화
- 추정: 파서/후보 생성 완화로 리콜이 올라간 대신, 제약 필터 강도가 약해졌을 가능성

### v2 -> v3 -> v4
- 확정: 지표 변화 거의 없음
- 추정: 구조/리팩토링 또는 내부 안정화 중심 변경

### v4 -> v5
- 확정: 하드 제약 위반률 0.1667→0.0, 추천 실패 2→0
- 추정: 제외 조건/필수 조건 처리 순서 또는 필터 강도 개선

### v5 -> v6
- 확정: nonempty_rate 1.0→0.8333, 추천 실패 0→2
- 추정: 필터가 과강화되어 일부 질의에서 후보 소실

### v6 -> v7
- 확정: nonempty_rate 회복(0.8333→1.0), 장르 적중률 상승(0.8333→0.9167), 실패 2→0
- 추정: v6에서 강해진 필터를 밸런싱해 리콜/정확도 동시 회복

### v7 -> v8
- 확정: 파서 성공률 급락(1.0→0.7375), parse_fail 0→20
- 확정: 추천 지표는 오히려 높게 유지(genre_hit 소폭 상승)
- 확정: parse_fail 질의가 특정 군(힐링 싱글 RPG, 빠른 액션, 난이도 높은 RPG 등)에 집중
- 추정: 평가 데이터/파서 판정 규칙이 확장되면서 파싱 기준이 더 엄격해졌거나,
  특정 표현군(붙여쓰기/강조문장/혼합조건)에 대한 룰 커버리지가 부족해짐

### v8 -> v9 -> v10
- 확정: 파서 성공률은 단계적으로 회복(0.7375→0.7833→0.8167)
- 확정: 장르 적중률 1.0 달성, 하드 위반률 0.0 도달, 추천 실패 0
- 확정: parse_fail 건수(20)는 유지
- 추정: 추천 단계(검색/재정렬/제약)는 크게 개선되었고,
  파서 자체는 일부 도메인에서 아직 미해결 이슈가 남은 상태

## 4) 실패 질의 관찰 포인트 (리포트 근거)

- 초반(v1) 파싱 실패:
  - `싱글로 천천히 할 수 있는 시뮬레이션` 변형군
- v8~v10 파싱 실패 집중:
  - `힐링되는 싱글 RPG... 공포는 싫어` 변형군
  - `빠르게 몰아치는 액션...` 변형군
  - `난이도 높은 RPG...` 변형군
- 해석:
  - 단일 장르 질의보다 “복합 취향 + 제외 조건 + 표현 변형”에서 파싱 취약

## 5) 현재 시점 결론

- 추천 품질 지표(장르 적중/제약 위반/추천 실패)는 v10 기준 매우 안정적
- 파서 성공률은 회복 중이나, 특정 표현군 취약점이 남아 있음
- 대시보드/설명성 측면은 `abf9a21`에서 크게 강화되었고,
  `similar_to` DB 미매칭 fallback(LLM profile)까지 반영됨

## 6) 주의 (정확성 범위)

- v1~v10 세부 작업 의도는 채팅 로그가 아니라 지표/코드 변화로 추정한 부분이 포함됨
- 따라서 본 문서는 “정확한 회고 원문”이 아니라 “근거 기반 재구성 문서”임
